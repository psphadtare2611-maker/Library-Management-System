# ============================================================================
# reports/report_generator.py
# ----------------------------------------------------------------------------
# REPORT GENERATOR — formats and exports report data to files.
#
# Responsibilities (to be implemented later):
#   - export_to_csv()   -> write a report to reports/exports/*.csv
#   - export_to_excel() -> write a report to .xlsx (via openpyxl)
#   - export_to_pdf()   -> write a report to .pdf (via reportlab)
#
# Receives already-prepared data from report_service; this module only
# handles presentation/formatting into a file, not querying the database.
# ============================================================================
