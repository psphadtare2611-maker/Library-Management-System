# ============================================================================
# models/book.py
# ----------------------------------------------------------------------------
# BOOK ENTITY — a plain data class representing one row of the Books table.
#
# Will hold (to be implemented later) attributes such as:
#   book_id, title, author, isbn, category,
#   total_copies, available_copies, date_added, is_active
#
# Purpose: give the app a typed object to pass around instead of raw
# database tuples/dicts. Contains data (and maybe simple helpers), never SQL.
# ============================================================================
