# ============================================================================
# requirements.txt
# Python package dependencies for the Library Management System.
# Install with:  pip install -r requirements.txt
# ============================================================================

# --- Database connectivity ---
pyodbc>=5.0.0          # Connects Python to SQL Server via ODBC

# --- Reporting / exports (used by services/report_service + reports/) ---
# openpyxl>=3.1.0      # Export reports to Excel (.xlsx)  -- uncomment when needed
# reportlab>=4.0.0     # Export reports to PDF            -- uncomment when needed

# --- Notes ---
# Tkinter is NOT listed here: it ships with the standard Python installation.
# Keep this file trimmed to what is actually imported by the code.
